{
  "build_command": "pip install poetry && poetry config virtualenvs.create false && poetry install --no-dev --no-interaction",
  "start_command": "python -m uvicorn src.server:app --host 0.0.0.0 --port 8080"
}
