[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install poetry", "poetry config virtualenvs.create false", "poetry install --no-dev --no-interaction"]

[start]
cmd = "python -m uvicorn src.server:app --host 0.0.0.0 --port 8080"
